<div class="container">
    {{> header}} 
    {{#if hasTeam}}
    {{else}} 
        <a href="#/create" class="btn btn-default">Create Team</a>
    {{/if}}
    <h1>Catalog Page</h1>
    <div>
        {{#each teams}} 
            {{> teamItem}} 
        {{/each}}
    </div>
</div>
{{> footer}}